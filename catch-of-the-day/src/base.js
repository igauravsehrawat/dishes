import Rebase from 're-base';

var config = {
    apiKey: "secret-stuff",
    authDomain: "secret-stuff",
    databaseURL: "secret-stuffm"
}

var base = Rebase.createClass(config);

export default base;
